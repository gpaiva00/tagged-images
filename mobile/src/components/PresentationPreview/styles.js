import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    paddingVertical: 30,
    alignItems: 'center',
  },
  
  noContentView: {
    justifyContent: 'center'
  },
});